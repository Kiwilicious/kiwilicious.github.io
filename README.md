# Portfolio Site

Personal portfolio site written in TS React and styled-components. Deployed here on Github using gh-pages.

### Project Setup

This project uses [pnpm](https://pnpm.io/) as the package manager.

Standard commands to get started.
`pnpm i`
`pnpm dev`
